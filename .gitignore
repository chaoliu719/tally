# Common
*.log
.DS_Store
.env

# Go build output and local SQLite data
/mcp/tally-mcp
*.db

# Claude Code local state (per-machine, not shared)
.claude/settings.local.json

# Claude Code project settings (leave to each machine for now)
.claude/settings.json
